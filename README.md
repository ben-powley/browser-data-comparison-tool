# Browser data comparison tool

- Clone the repo
- Run `go install`
- Run `go run main.go` OR run `docker build -t browser-data-comparison-bool .` and then `docker run -it browser-data-comparison-tool`
